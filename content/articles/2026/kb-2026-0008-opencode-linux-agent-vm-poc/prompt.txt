OpenCodeをLinux Agent VMへ導入し、WindowsからSSH経由で実用的に開発できるかを検証してください。

前提:
- AIエージェント本体、Git、Docker、build/testはLinux VM側で実行する
- Windowsは操作UIとして使う
- OpenCode DesktopのNative Remote SSHが未実装である点を考慮する
- SSH上のTUIを直接使う方式に既知問題がある場合は、Web UI + SSH port forwardingを代替案として検証する

確認したい内容:
1. OpenCode CLIのLinux導入
2. OpenCode Go認証と日本語応答
3. localhost限定のOpenCode Web起動
4. Windows BrowserからSSH tunnel経由で接続
5. 日本語入力とclipboard
6. 実Repositoryのread-only調査
7. isolated環境でのファイル編集、build、test
8. Docker操作
9. Browser / SSH tunnel / Web server再起動後のセッション復旧
10. localhost限定公開、credential権限、push経路などのSecurity確認
11. cleanup
12. 実運用に必要なwrapperや回避策

結論から、問題点、対応方法、実際に試して分かった細かい点の順に整理してください。
公開時はホスト名、IP、ユーザー名、ローカルパスなどの環境固有情報を一般化してください。
