# AI回答の完全性区分

`article.json` の各エージェント回答に `integrity` を設定する。

| 値 | 意味 |
|---|---|
| `raw` | 原文 |
| `formatted` | 改行・見出し・コードブロックのみ調整 |
| `redacted` | 非公開情報をマスキング |
| `excerpted` | 一部省略 |
| `edited` | 内容を編集 |

`formatted` 以外の変更を行った場合、変更理由を記事内に明示する。

原文か編集済みか判断できない場合は停止する。
